/**
 * Application Startup File (cPanel / Phusion Passenger / Production)
 *
 * In cPanel "Setup Node.js App", this is set as the "Application startup file".
 * It loads the server entry point (compiled to JavaScript).
 */
import './server/index.js';
